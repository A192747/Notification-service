server:
  port: 8083

spring:
  application:
    name: message-handler-service
  kafka:
    bootstrap-servers: localhost:9092
    consumer:
      group-id: mailing
  servlet:
    multipart:
      max-file-size: 4MB
      max-request-size: 4MB

io:
  reflectoring:
    kafka:
      bootstrap-servers: localhost:9092


